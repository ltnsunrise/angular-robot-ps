import { CategoryPartTypePipe } from './category-part-type-pipe';

describe('CategoryPartTypePipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryPartTypePipe();
    expect(pipe).toBeTruthy();
  });
});
