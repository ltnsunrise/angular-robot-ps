import { GroupByIdPipe } from './group-by-id-pipe';

describe('GroupByIdPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupByIdPipe();
    expect(pipe).toBeTruthy();
  });
});
